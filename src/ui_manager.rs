use crate::error::LyricsifyError;

pub struct UIManager {
    // Implementation will be added in subsequent tasks
}

impl UIManager {
    pub fn new() -> Result<Self, LyricsifyError> {
        todo!("Implementation in tasks 6 and 7")
    }
}
